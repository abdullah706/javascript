//var array1 = [1, 2, 3, 4];

// pass a function to map
//var arr2 = array1.map(x => x + 10);

//console.log(arr2);

var array1 = [1, 2, 3, 4];

// pass a function to map
var arr2 = array1.map(function(x)){
    x => x + 10
    return  x;
}

console.log(arr2);
